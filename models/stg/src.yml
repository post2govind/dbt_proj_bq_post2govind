version: 2
sources:
   - name: src
     database: studious-bit-400118
     schema: input_schema
     tables:
         - name: customers
           description: '{{ doc("testmd")}}'
           columns:
             - name: CUSTOMERID
               tests:
                - unique
         - name: products
   - name: trans
     database: studious-bit-400118
     schema: trans_schema
     tables:
         - name: orders